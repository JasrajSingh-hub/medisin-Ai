# MediSign-AI — Emergency Gesture Recognition
# Runtime + training dependencies.
#
# NOTE: This environment (dev sandbox) has no outbound network, so the stack
# could not be installed here. Install locally with:
#     python -m venv .venv
#     .venv\Scripts\activate        (Windows)
#     pip install -r emergency/requirements.txt
#
# Python >= 3.11 (uses stdlib tomllib for config; 3.12 used in development).

# --- Core scientific stack -------------------------------------------------
numpy>=1.24
opencv-python>=4.8
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3

# --- Hand landmark extraction ---------------------------------------------
mediapipe>=0.10.0

# --- API service -----------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
python-multipart>=0.0.9

# --- Testing ---------------------------------------------------------------
pytest>=8.0
pytest-cov>=5.0
tqdm>=4.66
httpx>=0.27                 # required by FastAPI TestClient (starlette.testclient)
